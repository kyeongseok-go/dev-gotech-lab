import { PageContainer } from "@/components/layout/page-container";

export default function BlogPage() {
  return (
    <PageContainer>
      <h1 className="text-2xl font-semibold">블로그</h1>
      <p className="mt-3 text-muted-foreground">글 목록이 여기에 표시됩니다. (Day 3에서 구현)</p>
    </PageContainer>
  );
}
